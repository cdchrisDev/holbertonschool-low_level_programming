/*      int sign;
        unsigned int val;

        val = 0;
        sign = 1;

        while (*s++ != '\0')
        {
                if (*s == '-')
                        sign *= -1;

                if (*s >= '0' && *s <= '9')
                        val = (val * 10) + (*s - '0');

                else if (val > 0)
                        break;
        }
        return (sign * val);
*/
