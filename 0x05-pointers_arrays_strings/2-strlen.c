#include "holberton.h"
/**
 *_strlen - lenght of the string
 *@s: lenght of the value
 *Return: int returned
 */
int _strlen(char *s)
{
	int i;

	/* array condition in with value i */
	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}

