#include "main.h"
/**
 * _abs - 2th func called
 * print the absolute value of a int
 * @n: a int
 * Return: 0 or -1
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
