#include "main.h"
/**
 * _isdigit - 2th func called
 * a program to check for ints
 * @c: a int
 * Return: 1 if digit, 0 if not
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);

	else
		return (0);

	return (c);
}
